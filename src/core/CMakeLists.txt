set(CURRENT_LIB core)

FILE(GLOB_RECURSE ${CURRENT_LIB}_SOURCES "*.cpp")
FILE(GLOB_RECURSE ${CURRENT_LIB}_HEADERS "${CMAKE_SOURCE_DIR}/include/atlas/${CURRENT_LIB}/*.hpp")

set(HEADERS ${HEADERS} ${${CURRENT_LIB}_HEADERS} PARENT_SCOPE)
set(SOURCES ${SOURCES} ${${CURRENT_LIB}_SOURCES} PARENT_SCOPE)

